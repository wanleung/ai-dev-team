server:
  host: 0.0.0.0
  port: 8765
  api_key: "change-me"           # override with AISW_API_KEY env var

defaults:
  repo: "owner/default-repo"
  pipeline: "ai-feature"
  engineers: 2
  config_yaml: "config.yaml"
